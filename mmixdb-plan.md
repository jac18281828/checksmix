# mmixdb — Plan

`mmixdb` is a gdb-style debugger for MMIX programs assembled by this repo,
built in two prompts:

- **pc.1 — source-line debug-info substrate.** Adds an address ↔ original
  source-location map (file + line) and access to the original source text
  on `MMixAssembler`. Independently mergeable: public API + tests only,
  nothing a user observes when running programs changes.
- **pc.2 — the `mmixdb` REPL binary.** A gdb-style command-line debugger
  (breakpoints, step/step-over, backtrace) built on top of pc.1's API, with
  `rustyline` for line editing and an Emacs/GUD `--fullname` marker output
  mode for in-editor debugging.

## Status

### pc.1 — source-line debug-info in the assembler: **DONE**

- **Date:** 2026-07-05
- **Branch:** `claude/mmixdb-debug-info`

Landed on `MMixAssembler` (`src/mmixal.rs`), re-exported from `src/lib.rs`:

- `pub struct SourceLoc { pub file: String, pub line: usize }`
- `pub fn source_loc(&self, addr: u64) -> Option<&SourceLoc>`
- `pub fn addr_for_line(&self, file: &str, line: usize) -> Option<u64>`
- `pub fn source_text(&self, file: &str, line: usize) -> Option<&str>`

Internals: `preprocess_debug` now emits a labeled `debug` directive's label
and generated `PUSHJ` on a single preprocessed line (was two), so every
original source line maps to exactly one preprocessed line and pest's
`line_col()` reports the user's real line number. Each translation unit
retains its original (un-preprocessed) text and original line count
alongside the preprocessed text the parser walks, so pass 2 can build
`debug_info: BTreeMap<u64, SourceLoc>` — recording an entry only for lines at
or below that boundary, so instructions generated by the appended
debug-subroutine block get no entry.

pc.2 can rely on these four names (`SourceLoc`, `source_loc`,
`addr_for_line`, `source_text`) as the stable API surface from pc.1.

### pc.2 — the `mmixdb` REPL binary: **TODO**

Not started. Will consume pc.1's API; no debugger/binary/REPL logic exists
yet anywhere in the repo.
